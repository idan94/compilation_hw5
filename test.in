void printByValue(bool val) {
	if (val)
		print("val is true");
	else {
		print("val is false");
	}
 return;
}

void main() {
  int a = 15;
  int c = 20;
  int d = a + c;
  
  int e = c/a;
  
  bool boolean;
  boolean = (a<c);
  printByValue(boolean);
	printByValue(true);
	printByValue(false);
      
}